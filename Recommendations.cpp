#include <bits/stdc++.h>
using namespace std;

int main()
{
    /* Faster IO */
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    /* Faster IO */

    unsigned long int n, i;
    unsigned long long m, x;
    vector<unsigned long long> a, t, s, d;

    cin >> n;

    for (i = 0; i < n; i++)
    {
        cin >> m;
        a.push_back(m);
    }
    for (i = 0; i < n; i++)
    {
        cin >> m;
        t.push_back(m);
    }

    return 0;
}